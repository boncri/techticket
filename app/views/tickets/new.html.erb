<h1><%= t('.title', customer: @customer.name) %></h1>

<%= simple_form_for([@customer, @ticket]) do |f| %>
	<%= render 'form', f: f, customer: @customer %>
<% end %>

<%= link_to t('back'), customer_tickets_path(@customer) %>
